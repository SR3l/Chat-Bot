const CustomLoader = () => {
  return (
    <div className="absolute h-screen w-screen top-0 left-0 flex items-center justify-center backdrop-blur-sm bg-black/10">
      <span className="loader"></span>
    </div>
  );
};

export default CustomLoader;
